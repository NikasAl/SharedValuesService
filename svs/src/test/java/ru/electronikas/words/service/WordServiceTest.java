package ru.electronikas.words.service;
/*

import Parameter;
import ParameterService;
import org.junit.Assert;
import org.junit.Ignore;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import java.util.List;

@RunWith(SpringJUnit4ClassRunner.class)
//@ContextConfiguration(locations = {"classpath:*//*
root-context.xml"})
public class WordServiceTest {

    @Autowired
    ParameterService parameterService;

    @Test
    @Ignore
    public void insertWordTest() {
        List<Parameter> words = parameterService.listParametersByUser("user1");
        int size = words.size();

        Parameter parameter = new Parameter();
        parameter.setName("nm");
        parameter.setValue("vl");
        parameterService.add(parameter);

        words = parameterService.listParametersByUser("user1");
        Assert.assertEquals(size + 1,words.size());
    }

}
*/
